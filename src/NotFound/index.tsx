import { Component } from 'react'

type Props = object;
type State = object;

export default class NotFound extends Component<Props, State> {
    public render () {
        return (
            <div>NotFound</div>
        )
    }
}
